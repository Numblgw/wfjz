<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.numb.wfjz.mapper.AdminMapper">
    <select id="selectUserDetailList" resultType="userDetail">
        select u.id id , u.username username ,
        u_d.nickname nickname , u_d.sex sex , u_d.email email , u_d.phone phone , u_d.address address
        from wfjz_user u,wfjz_user_detail u_d
        where u.id = u_d.user_id
    </select>
</mapper>